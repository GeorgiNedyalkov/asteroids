# Development Notes

## Session 2 - Math

- Get the forward vector

We have an angle in degrees. First we turn it to radians.
Then to get the vector from the angle we have the cos(angle) for x and sin(angle) for y.

Screen space world space still not understood.
Same goes for the matrices.


## Session 2.1 - Psysics

Speed

`Velocity` - An object's speed in a specific direction.
Vector representing the speed and direction.
The magnitude represents the speed - how fast and direction - where.

V = P / T;

Mass

`Accelaration` - The rate at which an object changes its velocity

a = v1 - v0 / t1 - t0

`Force`
